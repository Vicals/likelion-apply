<div class="container">
    <%= form_tag("/home/users", method: "post") do %>
        <div class="form-group">
          <%= label_tag(:email) %>
          <%= email_field_tag :email, nil, class: "form-control" %>
        </div>

        <div class="form-group">
          <%= label_tag(:password) %>
          <%= password_field_tag :password, nil, class: "form-control" %>
          <%= submit_tag "확인", class: "btn btn-light col-2", style: "margin-top:50px;" %>
        </div>
    <% end %>
    제작 : 경북대학교 멋쟁이 사자처럼 이우섭
</div>
